name: steve_tester

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: just echo a message
      run: |
        echo Hello there!

    - name: ping google
      run: |
        ping www.google.com
